---
summary: "CLI reference for `Agent-I clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `Agent-I clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `Agent-I clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `Agent-I clawbot qr` (same behavior as [`Agent-I qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `Agent-I clawbot qr` -> `Agent-I qr`
